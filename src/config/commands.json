{
  "commands": [
    {
      "name": "pick",
      "description": "Analyze a bet slip and provide VIP play analysis",
      "options": [
        {
          "name": "image",
          "description": "Bet slip image to analyze",
          "type": "ATTACHMENT",
          "required": true
        }
      ]
    },
    {
      "name": "admin",
      "description": "Admin commands",
      "options": [
        {
          "name": "ping",
          "description": "Test bot responsiveness",
          "type": "SUB_COMMAND",
          "required": true
        },
        {
          "name": "status",
          "description": "Check bot and system status",
          "type": "SUB_COMMAND",
          "required": true
        }
      ]
    }
  ]
} 