/**
 * Example utility function for image processing.
 * @param {Buffer} buffer
 * @returns {Buffer}
 */
export function exampleHelper(buffer) {
  // Placeholder for future helpers
  return buffer;
} 